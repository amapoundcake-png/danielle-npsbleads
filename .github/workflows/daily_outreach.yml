name: Daily Lead Outreach
# DISABLED -- Railway handles scheduling now
on:
  workflow_dispatch:  # manual trigger only, no schedule
jobs:
  daily:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. Railway scheduler handles daily outreach."
