name: Follow-up Emails
# DISABLED -- Railway handles scheduling now
on:
  workflow_dispatch:  # manual trigger only, no schedule
jobs:
  followup:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. Railway scheduler handles follow-ups."
